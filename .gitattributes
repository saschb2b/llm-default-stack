# Normalize line endings: store text as LF in the repo, check out native.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary

# Lockfile: large, generated — collapse in diffs.
pnpm-lock.yaml -diff linguist-generated
