---
title: The explorer
description: Browse the default React stack each LLM, builder, and agent reaches for — filter by stack choice, open a model for its versions, or flip to the matrix and watch the columns collapse.
tableOfContents: false
---

import Explorer from '../../components/Explorer.astro';

Click any chip to filter every model that reaches for the same thing, or open a
model for its full version history and sources. Flip to **Matrix** to diff all of
them at once — the way you would diff the `package.json` files.

<Explorer />
