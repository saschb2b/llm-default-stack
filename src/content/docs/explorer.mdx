---
title: The explorer
description: Browse the default React stack each LLM and version reaches for — filter by model, version, or stack choice, or flip to the matrix and watch the columns collapse.
tableOfContents: false
---

import Explorer from '../../components/Explorer.astro';

Pick a model, switch between its versions, or click any chip to filter every model
that reaches for the same thing. Flip to **Matrix** to diff all of them at once —
the way you would diff the `package.json` files.

<Explorer />
